{
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "validate:data": "node scripts/validate-data.mjs",
    "validate:schedule": "node scripts/validate-schedule.mjs",
    "validate:images": "node scripts/prepare-images.mjs",
    "validate": "npm run test && npm run validate:data && npm run validate:schedule && npm run validate:images"
  },
  "devDependencies": {}
}

